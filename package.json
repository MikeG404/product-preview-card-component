{
  "name": "parcel-vanilla-starter",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
        "build": "parcel build src/index.html --dist-dir dist --public-url /product-preview-card-component/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.15.4",
    "gh-pages": "^6.3.0",
    "parcel": "^2.14.0"
  }
}
